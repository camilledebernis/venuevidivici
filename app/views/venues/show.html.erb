<%= render "venues/sidenav" %>

<div class="main">
  <div class="d-flex">
   <div class="col">
    <br></br>
    <h3><%= @venue.title  %></h3>
      <div class="card" style="width:200px">
        <div>
          <%= cl_image_tag(@venue.imgurl, cloud_name: 'danielcastro', width: 400, crop: "fill") %>
        </div>
      </div>
    </div>
    <div class="col">
        <div>
          <br></br>
          <br></br>
          <h5><%= @venue.description %></h5>
          <br>
          <p>Lorem Ipsum in french for description : Plusieurs variations de peuvent être trouvées ici ou là, mais la majeure partie d’entre elles a été altérée par l’addition d’humour ou de mots aléatoires qui ne ressemblent pas une seconde à du texte standard. Si vous voulez utiliser un passage du, vous devez être sûr qu’il n’y a rien d’embarrassant caché dans le texte. Tous les générateurs de sur Internet tendent à reproduire le même extrait sans fin.</p>
          <p class="paragraph"><%= @venue.price %>€ / day</p>
        </div>
    </div>
  </div>
  <div class="container">
    <div class="justify-content-center" style="width:500px">
      <%= render "bookings/form", venue: @venue, booking: @booking %> <!-- THIS IS THE FORM FOR BOOKINGS NEW -->
    </div>
  </div>
  <div class="mt-5">
    <%= link_to 'Go back', venues_path(:search => {query: ""}), class: 'btn btn-link' %>
    <%# link_to 'Go back', venues_path(:search => {query: @venue.place}), class: 'btn btn-primary' %>
  </div>



</div>
