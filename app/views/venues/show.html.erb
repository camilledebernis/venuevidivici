
<%= render "venues/sidenav" %>
<%= render "venues/topnav"%>

<div class="main">
  <div class="d-flex">
   <div class="col">
      <div class="card" style="width:200px">
        <div>
          <%= cl_image_tag("street_festival_syipf1", cloud_name: 'danielcastro', height: 300, width: 300, crop: "fill") %>
        </div>
      </div>
    </div>
    <div class="col">
        <div>
          <h5>Street Festival</h5>
          <br>
          <p>Plusieurs variations mais la majeure partie d'entre elles a été altérée par l'addition d'humour ou de mots aléatoires qui ne ressemblent pas une seconde à du texte standard. Si vous voulez utiliser un passage rien d'embarrassant caché dans le texte. Tous les générateurs de combinaison de plusieurs structures de phrases, pour générer irréprochable. Le ainsi obtenu ne contient aucune répétition, ni ne contient des mots farfelus, ou des touches d'humour.</p>
          <p class="paragraph">800€ / day</p>
        </div>
    </div>
  </div>
  <div class="container">
    <div class="justify-content-center" style="width:500px">
      <%= render "bookings/form", venue: @venue, booking: @booking %> <!-- THIS IS THE FORM FOR BOOKINGS NEW -->
    </div>
  </div>
  <div class="mt-5">
    <%= link_to 'Go back', venues_path(:search => {query: ""}), class: 'btn btn-primary' %>
    <%# link_to 'Go back', venues_path(:search => {query: @venue.place}), class: 'btn btn-primary' %>
  </div>



</div>
